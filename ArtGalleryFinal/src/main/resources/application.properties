spring.application.name=ArtGalleryFinal

# MySQL Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/onlineArtGallery_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=Chinni@3456
server.port=8081

# Server port inside container
server.port=8081

# JPA / Hibernate settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Disable cloud discovery and Eureka
spring.cloud.discovery.enabled=false
eureka.client.enabled=false
